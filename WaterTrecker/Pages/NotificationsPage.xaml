<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WaterTrecker.Pages.NotificationsPage"
             Title="Напоминания">

    <ScrollView>
        <VerticalStackLayout Padding="20,16" Spacing="16">

            <!-- ЗАГОЛОВОК -->
            <Label Text="Напоминания"
                   Style="{StaticResource DisplayLabel}"
                   HorizontalTextAlignment="Start"
                   FontSize="28"/>

            <!-- ВКЛЮЧИТЬ НАПОМИНАНИЯ -->
            <Border Style="{StaticResource Card}">
                <Grid ColumnDefinitions="*,Auto">
                    <VerticalStackLayout Grid.Column="0"
                                         Spacing="4"
                                         VerticalOptions="Center">
                        <Label Text="Напоминания включены"
                               Style="{StaticResource BodyLabel}"
                               FontAttributes="Bold"/>
                        <Label Text="Напоминать пить воду в течение дня"
                               Style="{StaticResource CaptionLabel}"/>
                    </VerticalStackLayout>
                    <Switch Grid.Column="1"
                            IsToggled="{Binding Settings.Enabled}"
                            OnColor="#1A9BE8"
                            ThumbColor="White"
                            VerticalOptions="Center"/>
                </Grid>
            </Border>

            <!-- ИНТЕРВАЛ -->
            <Label Text="Интервал"
                   Style="{StaticResource SectionLabel}"/>

            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="16">
                    <Label Text="{Binding IntervalText}"
                           TextColor="#1A9BE8"
                           FontSize="22"
                           FontAttributes="Bold"
                           HorizontalTextAlignment="Center"/>
                    <Label Text="Напоминание будет приходить с этим интервалом"
                           Style="{StaticResource CaptionLabel}"
                           HorizontalTextAlignment="Center"/>

                    <!-- Кнопки изменения интервала -->
                    <Grid ColumnDefinitions="56,*,56"
                          ColumnSpacing="20"
                          HorizontalOptions="Center"
                          WidthRequest="220">

                        <Button Grid.Column="0"
                                Text="−"
                                Clicked="MinusClicked"
                                BackgroundColor="#E8F4FD"
                                TextColor="#1A9BE8"
                                FontSize="22"
                                FontAttributes="Bold"
                                CornerRadius="28"
                                HeightRequest="56"
                                WidthRequest="56"
                                Padding="0"
                                HorizontalOptions="Center">
                            <Button.Shadow>
                                <Shadow Brush="Transparent" Offset="0,0" Radius="0" Opacity="0"/>
                            </Button.Shadow>
                        </Button>

                        <Label Grid.Column="1"
                               Text="мин"
                               Style="{StaticResource CaptionLabel}"
                               VerticalOptions="Center"
                               HorizontalTextAlignment="Center"/>

                        <Button Grid.Column="2"
                                Text="+"
                                Clicked="PlusClicked"
                                BackgroundColor="#1A9BE8"
                                TextColor="White"
                                FontSize="22"
                                FontAttributes="Bold"
                                CornerRadius="28"
                                HeightRequest="56"
                                WidthRequest="56"
                                Padding="0"
                                HorizontalOptions="Center">
                            <Button.Shadow>
                                <Shadow Brush="#401A9BE8" Offset="0,3" Radius="8" Opacity="0.25"/>
                            </Button.Shadow>
                        </Button>

                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- ТИХИЕ ЧАСЫ -->
            <Label Text="Тихие часы"
                   Style="{StaticResource SectionLabel}"/>

            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="14">
                    <Label Text="Не беспокоить в это время"
                           Style="{StaticResource CaptionLabel}"/>

                    <Grid ColumnDefinitions="*,Auto,*" ColumnSpacing="10">

                        <Border Grid.Column="0"
                                BackgroundColor="{AppThemeBinding Light=#F0F8FF, Dark=#1C2F42}"
                                StrokeShape="RoundRectangle 12"
                                Stroke="{AppThemeBinding Light=#D6EAF8, Dark=#2A4258}"
                                StrokeThickness="1"
                                Padding="12,0"
                                HeightRequest="48">
                            <TimePicker Time="{Binding Settings.QuietStart}"
                                        Format="HH:mm"
                                        TextColor="{AppThemeBinding Light=#0D2B45, Dark=#E8F4FD}"
                                        BackgroundColor="Transparent"
                                        HorizontalOptions="Center"
                                        HorizontalTextAlignment="Center"
                                        VerticalOptions="Center"/>
                        </Border>

                        <Label Grid.Column="1"
                               Text="—"
                               TextColor="{AppThemeBinding Light=#5A87A8, Dark=#7AABCC}"
                               FontSize="16"
                               VerticalOptions="Center"
                               HorizontalTextAlignment="Center"/>

                        <Border Grid.Column="2"
                                BackgroundColor="{AppThemeBinding Light=#F0F8FF, Dark=#1C2F42}"
                                StrokeShape="RoundRectangle 12"
                                Stroke="{AppThemeBinding Light=#D6EAF8, Dark=#2A4258}"
                                StrokeThickness="1"
                                Padding="12,0"
                                HeightRequest="48">
                            <TimePicker Time="{Binding Settings.QuietEnd}"
                                        Format="HH:mm"
                                        TextColor="{AppThemeBinding Light=#0D2B45, Dark=#E8F4FD}"
                                        BackgroundColor="Transparent"
                                        HorizontalOptions="Center"
                                        HorizontalTextAlignment="Center"
                                        VerticalOptions="Center"/>
                        </Border>

                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- БЛИЖАЙШИЕ УВЕДОМЛЕНИЯ -->
            <Label Text="Ближайшие уведомления"
                   Style="{StaticResource SectionLabel}"/>

            <Border Style="{StaticResource Card}" Padding="16">
                <CollectionView ItemsSource="{Binding UpcomingNotifications}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,8"
                                    BackgroundColor="{AppThemeBinding Light=#E8F4FD, Dark=#1C2F42}"
                                    StrokeShape="RoundRectangle 10"
                                    StrokeThickness="0"
                                    Padding="14,10">
                                <Grid ColumnDefinitions="Auto,*">
                                    <Label Grid.Column="0"
                                           Text="🔔"
                                           FontSize="16"
                                           VerticalOptions="Center"
                                           Margin="0,0,10,0"/>
                                    <Label Grid.Column="1"
                                           Text="{Binding}"
                                           TextColor="{AppThemeBinding Light=#0D2B45, Dark=#E8F4FD}"
                                           FontSize="15"
                                           VerticalOptions="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
